"""Scenario library — each module contributes one safe SimulationScenario.

Discovered by ``simcore.registry``. Keep one capability per file (no monoliths);
every scenario here is inspection-only and MITRE ATT&CK-mapped.
"""
