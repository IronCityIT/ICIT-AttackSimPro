{
  "name": "attacksimpro-functions",
  "description": "Iron City AttackSimPro Cloud Functions (storeScanResults ingest)",
  "version": "1.1.0",
  "private": true,
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check handler.js && node --check local-server.js",
    "test": "node --test",
    "start:local": "node local-server.js",
    "smoke": "bash ../scripts/smoke.sh",
    "deploy": "echo \"Deploy runs in .github/workflows/deploy-functions.yml\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
